// import { NextFunction, Request, Response } from 'express';
// // Full user info, not just what is supplied by the /profile route.
// export const readUser = async (req: Request, res: Response, next: NextFunction): Promise<void> => {
//   res.status(200).json({ req.oidc.user });
// };

// export default {
//   readUser,
// };
